machine:
  node:
    version: 0.12.0

dependencies:
  pre:
    - pip install -r requirements.txt
  override:
    - npm install
    - eb init senate-project -p Node.js -r us-east-1

deployment:
    public:
      branch: master
      commands:
        - npm run build
        - eb deploy senateproject-dev
